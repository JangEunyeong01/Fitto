# 이미지 빌드에 보낼 필요가 없는 것들.
# Dockerfile은 필요한 파일만 골라 복사하지만, 여기서 빼지 않으면 빌드 도구에는 전부 넘어간다.

# 로컬 빌드 결과. 이미지 안에서 다시 빌드한다.
build/
.gradle/

# 실제 값이 든 환경변수 파일. 이미지에 들어갈 일은 없지만 빌드 도구에 넘길 이유도 없다.
.env
.env.*
!.env.example

# 편집기 설정
.idea/
*.iml
.vscode/
